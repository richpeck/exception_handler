source 'https://rubygems.org'

# Specify your gem's dependencies in custom_error_pages.gemspec
gemspec

#Haml (for layout - if user has application layout in HAML)
gem 'haml', '~> 4.0.5'

###########################################

#For Testing
group :test do
	gem 'rspec-rails'
	gem 'rails', '~> 4.1.1'
	gem 'coveralls', require: false
end

###########################################
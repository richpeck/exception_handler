<!DOCTYPE html>
<html>
  <head>

    <!--Info -->
    <title><%= "Code #{@status} (#{details[:name]}) | #{@app_name.upcase}" %></title>
    <%= stylesheet_link_tag "exception_handler/error" %>

  </head>
  
  <!--Body -->
  <body>
    <div class="container">
      <%= yield %>
    </div>

    <!-- Footer -->
    <footer>
    	<div class="connect">
    		<% ExceptionHandler.config[:social].each do |key,value| %>
    			<%= link_to image_tag("exception_handler/connect/#{key}.png", title: "Find us on " + key.to_s.titleize), value.join("/"), target: :blank, class: key.to_s %>
    		<% end %>
    	</div>
    	<div class="app">
    		<strong><%= link_to @app_name, "google.com", title: "Go to index" %></strong>
    		<span> - <%= @status %> Error</span>
    	</div>
    </footer>

  </body>
</html>